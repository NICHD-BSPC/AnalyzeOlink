% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{prepare_covariate_coldata}
\alias{prepare_covariate_coldata}
\title{Prepare covariate colData for each contrast}
\usage{
prepare_covariate_coldata(se_list, contrast_specs)
}
\arguments{
\item{se_list}{Named list of SummarizedExperiment objects. The SE named in
each \code{contrast_specs[[i]]$se_object_name} must exist in \code{se_list}.}

\item{contrast_specs}{Named list describing each contrast. Each element must
include:
\itemize{
\item \code{se_object_name}  — name of SE in \code{se_list} (e.g., "Disease")
\item \code{sampletable_col} — grouping column in colData (default "Group")
\item \code{g1}              — first group label
\item \code{g2}              — second group label
Optionally:
\item \code{columns}         — character vector of colData columns to keep
(default keeps all).
}}
}
\value{
Named list. Each element is a list with components \code{df}, \code{col}, \code{g1}, \code{g2}.
}
\description{
Builds a named list of per-contrast metadata frames used in covariate analyses.
Each element contains:
\itemize{
\item \code{df}  — sample-level metadata for that contrast
\item \code{col} — grouping column to compare (default "Group")
\item \code{g1}  — first group label
\item \code{g2}  — second group label
}

This helper is intentionally explicit and user-configurable. It does \strong{not}
auto-detect covariates or groups. Test data expects the universal columns
\code{Group}, \code{Cov1}, and \code{Cov2}.
}
\examples{
contrast_specs <- list(
  disease_vs_control = list(
    se_object_name  = "Disease",
    sampletable_col = "Group",
    g1 = "disease",
    g2 = "control",
    columns = c("SampleID", "Group", "Cov1", "Cov2")
  )
)
coldata_list <- prepare_covariate_coldata(se_list, contrast_specs)

}
