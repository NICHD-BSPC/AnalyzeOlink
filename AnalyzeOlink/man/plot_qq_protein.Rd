% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assumptions.R
\name{plot_qq_protein}
\alias{plot_qq_protein}
\title{QQ plots of residuals by p-value bin (per protein)}
\usage{
plot_qq_protein(
  res_list,
  outdir,
  label_map = NULL,
  num_proteins = 10,
  bins = c(0, 0.1, 0.25, 0.5, 0.75, 1),
  seed = 1
)
}
\arguments{
\item{res_list}{Named list from \code{run_tests()}; each element has \verb{$anova}
with columns \code{Assay}, \code{Panel}, \code{SW_pval}, and list-column \code{resids}.}

\item{outdir}{String; output directory for PDFs (must exist).}

\item{label_map}{Optional list; \code{label_map[[contrast]]} is a named list whose
names are \code{SampleID}s and whose values are character vectors of
\code{"Assay_Panel"} to label in the QQ plot.}

\item{num_proteins}{Integer; max proteins per bin (default \code{10}).}

\item{bins}{Numeric vector of cut points in (0,1] (default \code{c(0,0.1,0.25,0.50,0.75,1)}).}

\item{seed}{Integer; RNG seed for reproducibility.}
}
\value{
\code{invisible(NULL)}; side effects are plots, PDFs, and markdown.
}
\description{
Builds a tabset per contrast, subdivides proteins into p-value bins (by
\code{SW_pval}), and for up to \code{num_proteins} per bin draws QQ plots of model
residuals. Optional labels can be added via \code{label_map}.
}
