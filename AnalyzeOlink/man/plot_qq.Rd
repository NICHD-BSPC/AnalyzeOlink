% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assumptions.R
\name{plot_qq}
\alias{plot_qq}
\title{Residual QQ plots by protein or by sample}
\usage{
plot_qq(
  res_list,
  outdir,
  label_map = NULL,
  num_proteins = 10,
  num_samples = 10,
  type = c("protein", "sample"),
  freq_table = NULL,
  bins = c(0, 0.1, 0.25, 0.5, 0.75, 1),
  seed = 1
)
}
\arguments{
\item{res_list}{Named list from \code{run_tests()}; each element has \verb{$anova}
with residuals.}

\item{outdir}{String; output directory for PDFs (must exist).}

\item{label_map}{Optional list of outliers:
\code{label_map[[contrast]][[SampleID]] = c("Assay_Panel", ...)}.}

\item{num_proteins}{Integer; number of proteins to show per bin/sample (default \code{10}).}

\item{num_samples}{Integer; number of samples to display (default \code{10}).}

\item{type}{\code{"protein"} or \code{"sample"} (default).}

\item{freq_table}{When \code{type = "sample"}, a data frame or list of data frames
with columns \code{SampleID} and \code{Frequency} (and optionally \code{Contrast}).}

\item{bins}{Numeric vector of p-value cut points (used only for \code{type="protein"}).}

\item{seed}{Integer RNG seed.}
}
\value{
\code{invisible(NULL)}; side effects are plots, PDFs, and markdown.
}
\description{
Wrapper that either (i) delegates to \code{plot_qq_protein()} for per-protein,
p-value-binned QQ plots, or (ii) for \code{type = "sample"}, selects the top
\code{num_samples} by outlier frequency and, for each, draws QQ plots labeling
all sample outliers for selected proteins.
}
