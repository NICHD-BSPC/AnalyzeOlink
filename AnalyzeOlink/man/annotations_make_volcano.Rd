% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.R
\name{annotations_make_volcano}
\alias{annotations_make_volcano}
\title{Build an annotated volcano plot (PDF + interactive Plotly)}
\usage{
annotations_make_volcano(
  res,
  se_list,
  contrast,
  test_type,
  outdir,
  parameters,
  color_by,
  export_2nd = FALSE,
  export_2nd_y_lim = 5,
  vert_line = TRUE
)
}
\arguments{
\item{res}{Results data frame containing at least \code{Adjusted_pval} and either
\code{estimate} (preferred) or \code{log2FC}.}

\item{se_list}{Named list of \code{SummarizedExperiment} objects (unused here, reserved).}

\item{contrast}{Contrast name (string) used in titles/filenames.}

\item{test_type}{Test name key (used for sheet titles and filenames).}

\item{outdir}{Output directory (created if needed).}

\item{parameters}{List with \code{treatment}, \code{control}, and \code{alpha}.}

\item{color_by}{Annotation column to map to color (supports 0/1 or 0–3 integers).}

\item{export_2nd}{Logical; also write a zoomed PDF limited by \code{export_2nd_y_lim}.}

\item{export_2nd_y_lim}{Numeric; y-axis (–log10 p) limit for the zoomed plot.}

\item{vert_line}{Logical; draw a vertical line at x = 0.}
}
\value{
Invisibly, a list with \code{ggplot}, \code{p_plotly}, \code{pdf_primary}, \code{pdf_secondary}.
}
\description{
Creates a volcano plot for a single contrast + test type, colored by an
annotation column (binary 0/1 or graded 0–3). Writes a PDF and returns the
ggplot object, a Plotly widget, and output paths.
}
